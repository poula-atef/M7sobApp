<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.m7sob">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_motorcycle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_motorcycle"
        android:supportsRtl="true"
        android:theme="@style/Theme.M7sob">
        <activity android:name=".OrdersActivity"></activity>
        <activity android:name=".innerActivity" />
        <activity android:name=".paymentsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>